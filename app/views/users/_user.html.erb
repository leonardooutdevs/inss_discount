<%= turbo_frame_tag dom_id(user) do  %>
  <div class='row'>
    <div class='col'>
      <%= user.email %>
    </div>
    <div class='col'>
      <%= l user.updated_at, format: :short %>
    </div>
    <div class='col'>
      <div class="btn-group btn-group-sm" role="group">
        <%= link_to t('edit'),
            url_for([:edit, user]),
            class: 'btn btn-outline-primary',
            method: :get, format: :turbostream,
            data: { turbo: true } %>
        <%= link_to t('accesses'),
            user_access_permission_levels_path(user),
            class: 'btn btn-outline-primary',
            data: { turbo: false } %>
      </div>
    </div>
  </div>
<% end %>
